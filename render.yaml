services:
  - type: web
    name: sirius-pulse
    runtime: python
    buildCommand: cd backend && pip install -r requirements.txt --force-reinstall
    startCommand: cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT
    plan: free
    autoDeploy: true
